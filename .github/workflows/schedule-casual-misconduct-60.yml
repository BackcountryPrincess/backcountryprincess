name: Schedule Casual Misconduct 60 Posts

# Disabled on 2026-08-28 after review found this workflow could create
# scheduled Facebook posts for Casual Misconduct. Do not re-enable until Jenn
# has approved the exact assets, captions, destinations, timing, and cross-post
# plan.

on:
  workflow_dispatch:
    inputs:
      reason:
        description: "Disabled placeholder only. This workflow does not schedule posts."
        required: false
        type: string

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Scheduling disabled
        run: |
          echo "Casual Misconduct static queue automation is disabled."
          echo "No Facebook scheduling occurs from this workflow."
