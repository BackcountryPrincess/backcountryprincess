name: Build dist.zip (Vite)

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install
        working-directory: src
        run: npm ci

      - name: Build
        working-directory: src
        run: npm run build

      # Create dist.zip from the contents of src/dist (zip contains index.html + assets/, not a nested dist/ folder)
      - name: Create dist.zip
        working-directory: src/dist
        run: |
          rm -f ../../dist.zip
          zip -r ../../dist.zip .

      # Upload the ZIP you will deploy to GreenGeeks
      - name: Upload dist.zip artifact
        uses: actions/upload-artifact@v4
        with:
          name: dist-zip
          path: dist.zip

      # Optional: also upload the raw dist folder for inspection
      - name: Upload dist folder artifact
        uses: actions/upload-artifact@v4
        with:
          name: dist-folder
          path: src/dist
