name: Website deployment disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment intentionally disabled
        run: |
          echo "GreenGeeks deployment is intentionally disabled."
          echo "Target architecture: Cloudflare Worker serves maplesap.app, app.maplesap.app, and lab.maplesap.app."
