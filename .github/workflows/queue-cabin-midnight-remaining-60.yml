name: Queue Approved Casual Misconduct 60

# Disabled on 2026-08-28 after review found this workflow could create,
# verify, and replace scheduled Facebook posts for Casual Misconduct. Do not
# re-enable until Jenn has approved the exact assets, captions, destinations,
# timing, and cross-post plan.

on:
  workflow_dispatch:
    inputs:
      reason:
        description: "Disabled placeholder only. This workflow does not queue posts."
        required: false
        type: string

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Queue disabled
        run: |
          echo "Casual Misconduct queue reconciliation is disabled."
          echo "No Facebook scheduling, replacement, or deletion occurs from this workflow."
