# MapleSap iOS Wrapper

Fastest iOS path:
Native SwiftUI app with a WKWebView loading `https://maplesap.app`.

Bundle ID:
`com.smokeyriverstudio.maplesap.ios`

Device family:
iPhone and iPad.

Open:
`ios/MapleSap.xcodeproj`

Archive target:
`MapleSap`

Required Apple objects:
- iOS App ID for `com.smokeyriverstudio.maplesap.ios`
- iOS App Store provisioning profile
- App Store Connect iOS app record linked to that bundle ID

Before TestFlight:
- Fix live `maplesap.app` API and legal/support routes.
- Confirm App Privacy answers match production analytics/tracking.
- Replace placeholder support email.
